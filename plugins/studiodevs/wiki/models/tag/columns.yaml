# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: studiodevs.wiki::lang.models.tag.name
    color:
        label: studiodevs.wiki::lang.models.tag.name
        type: colorpicker
